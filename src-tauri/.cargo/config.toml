# macOS deployment target env vars moved to CI workflow (macOS-only steps)
# and the "package" npm script, because Cargo [env] is unconditional and
# breaks Linux builds by injecting -mmacosx-version-min into cc.
